CONTINUE THIS WORK — PLANO Agent Mesh v5 (MCP removed, CLI-first). You are taking over from the previous agent mid-session. Repo: D:/Tools/Plano (Electron infinite-canvas workspace app). Read first: CLAUDE.md, docs/engineering/plans/PLAN_AGENT_MESH_V5_CLI_FIRST.md, src/main/daemon/cli/, src/main/daemon/mesh/, .plano-tests/mesh-cli-e2e.mjs.

WHERE IT STANDS (mostly done, verified):
- The mesh is CLI-first. The MCP server is fully removed: mesh/mcp.ts deleted, `plano mcp` stdio gone, MCP provisioning gone, boot-time cleanupMcpEntries() strips stale `plano` MCP keys from harness configs. Native JSON-RPC endpoint POST /cli (mesh/endpoint.ts); /mesh kept as a compat alias.
- Full `plano` CLI (src/main/daemon/cli/, bundled to out/main/cli.js, installed into <userData>/bin by installCli, launchers run under ELECTRON_RUN_AS_NODE): whoami roster status inbox ack send ask reply cancel spawn "worktree create" wait claim handoff chain chain-payload chains cancel-chain broadcast context timeline find declare set-model interrupt compact agent-context help version. --json for machine output; PLANO_CLI_JSON=1; exit 2 = wait timeout.
- MeshBus.waitForIdle = plano_wait (event-driven long-poll; resolves on stable idle or exit; awaiting-input never counts as finished; `since` param). `plano send --wait` and `plano spawn --wait` wired (spawn --wait does an echo-poll on the prompt + 8s quiet window).
- Real-agent detection fixed on this machine: codex 0.147 runs as node_repl.exe (signature added in agentLight.ts + AgentDetectionService.ts), ProcessTreeService QUERY_TIMEOUT_MS raised 4000→15000 (cold PowerShell measured ~5s), daemon pre-warms the process-tree worker at boot.
- Docs: docs/architecture/ARCHITECTURE.md Agent Mesh section rewritten; the v5 plan doc exists. Claude skill (~/.claude/skills/plano-mesh/SKILL.md) is CLI-first.
- e2e: all .plano-tests/mesh-*.mjs migrated from MCP framing to native /cli. mesh-cli-e2e.mjs (C1-C9, boots the real app with temp userData + real codex with an isolated CODEX_HOME; answers codex trust/sandbox prompts): C1-C6, C8, C9 PASS, C5 PASSES (send --wait on real codex, delta > 0). C7 (spawn --wait with real codex) was the last open failure — a merged prompt-answering guard + 8s quiet window was just applied but the run was interrupted before it finished.

YOUR JOB — finish so everything works:
1. Iterate on `node .plano-tests/mesh-cli-e2e.mjs` until RESULT ok:true (C1-C9 all pass). The test is long (~6-10 min; it boots the app, spawns real codex twice, answers its trust/sandbox selects). Make the TEST robust (guards, waits, prompt answering) — never weaken the assertions on the mesh/CLI behavior. Note: kill leftover electron.exe test processes before reruns (command lines contain remote-debugging-port or daemon.js with Temp\mesh-), and the test already taskkills its own tree.
2. `npm run typecheck` (node + web) must pass clean.
3. Do NOT run dist/build packaging. Do NOT uninstall or touch the user's installed PLANO/Orca. Do NOT close the user's open PLANO app.
4. When green: start `npm run dev` (leave it running) so the new build is live in a dev instance, then report exactly what you changed and the final e2e result.
