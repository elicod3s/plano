# Demo media

Drop the launch media in here. Each slot below has a required filename and the exact spot where
it is referenced from the README. Record real footage of the app — no generated images.

<!-- ============================================================
  SLOT 1 — HERO (required)
  Filename: hero-canvas.mp4
  What: 15–30 s of the infinite canvas in motion — pan/zoom across a
  project with terminals, editor, browser and an agent panel side by
  side, ending on a clean frame of the whole layout.
  Referenced in: README.md, top ("What makes it different" / hero area).
============================================================ -->

<!-- ============================================================
  SLOT 2 — TERMINAL → AGENT MORPH (required)
  Filename: agent-morph.mp4
  What: 10–15 s of a plain terminal booting Claude Code / Codex and
  the panel morphing into agent mode (panel tint + breathing glow).
  Keep it tight: the morph is the moment.
  Referenced in: README.md, "Terminals that become agents".
============================================================ -->

<!-- ============================================================
  SLOT 3 — AGENTS TALKING (required)
  Filename: agents-talking.mp4
  What: 15–25 s of the mesh overlay (Ctrl+Shift+A): two agents on the
  canvas, one message fanned out, the reply flowing back — the
  persistent link line between panels if visible.
  Referenced in: README.md, "Agents that talk to each other".
============================================================ -->

<!-- ============================================================
  SLOT 4 — MOBILE REMOTE (optional)
  Filename: mobile-remote.mp4
  What: 10–15 s of the phone app viewing/talking to a running agent
  while the desktop app is closed.
  Referenced in: README.md, "Agents survive the app closing".
============================================================ -->

<!-- ============================================================
  SLOT 5 — CANVAS SCREENSHOT (optional)
  Filename: canvas-screenshot.png
  What: one 16:9 still of a busy project canvas, full opacity, no UI
  chrome cropped. Also usable as the social/OG image.
  Referenced in: README.md, hero area.
============================================================ -->

Conventions:

- MP4 (H.264 + AAC), 16:9, ≤ 30 s, silent unless narration is added later.
- Keep filenames exactly as written above so README references match.
- When a slot is filled, update README.md to embed it and remove the matching comment block.
